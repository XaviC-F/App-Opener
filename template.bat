echo off :: stops the interface from displaying the code it's executing
"[app1 path]" :: replace [file path] with the path to a .exe file, e.g. "C:\Program Files\Chrome.exe"
"[app2 path"
exit :: closes the command line interface
